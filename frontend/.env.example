# API Configuration
# The base URL for the backend API
# For local development: http://localhost:3333
# For production: https://your-domain.com/api or http://your-server-ip:3333
# For network access: http://192.168.1.x:3333 (replace with your local IP)
VITE_API_URL=http://localhost:3333

# WebSocket Configuration  
# The URL for WebSocket connections
# Should match the API URL in most cases
VITE_WS_URL=http://localhost:3333

# Development configuration
NODE_ENV=development
