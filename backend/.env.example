# Časová zóna
TZ=UTC

# Nastavenie servera
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info

# APP KEY pre AdonisJS
# Vygeneruj príkazom:
# docker compose run --rm backend node ace generate:key
APP_KEY=

# Režim aplikácie (development / production)
NODE_ENV=development

# Databáza (matchuje docker-compose nastavenia)
DB_HOST=db
DB_PORT=5432
DB_USER=root
DB_PASSWORD=root
DB_DATABASE=app

# CORS - Allowed Origins (comma-separated)
# Pre development: automaticky povolí všetky origins
# Pre production: špecifikuj povolené origins
# Príklady:
#   - http://localhost:9000,http://127.0.0.1:9000
#   - http://192.168.*.*:9000 (pre lokálnu sieť s wildcard)
#   - https://your-domain.com
ALLOWED_ORIGINS=http://localhost:9000,http://127.0.0.1:9000,http://192.168.*.*:9000