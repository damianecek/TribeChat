# Time zone
TZ=UTC

# Server settings
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info

# APP KEY for AdonisJS
# Generate with:
# docker compose run --rm backend node ace generate:key
APP_KEY=

# Application mode (development / production)
NODE_ENV=development

# Database (matches docker-compose settings)
DB_HOST=db
DB_PORT=5432
DB_USER=root
DB_PASSWORD=root
DB_DATABASE=app

# CORS - Allowed Origins (comma-separated)
# For development: automatically allows all origins
# For production: specify allowed origins
# Examples:
#   - http://localhost:9000,http://127.0.0.1:9000
#   - http://192.168.*.*:9000 (for local network with wildcard)
#   - https://your-domain.com
ALLOWED_ORIGINS=http://localhost:9000,http://127.0.0.1:9000,http://192.168.*.*:9000