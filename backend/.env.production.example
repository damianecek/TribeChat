# Production Environment Configuration
# ==========================================
# IMPORTANT: Update these values for your production deployment
# ==========================================

# Time zone
TZ=UTC

# Server settings
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info

# APP KEY for AdonisJS
# CRITICAL: Generate a new key for production!
# docker compose run --rm backend node ace generate:key
APP_KEY=

# Application mode
NODE_ENV=production

# Database - Use strong passwords for production!
DB_HOST=db
DB_PORT=5432
DB_USER=root
DB_PASSWORD=root
DB_DATABASE=app

# CORS - Allowed Origins (comma-separated)
# For production, specify exact domains or IP addresses
# Examples:
#   - https://your-domain.com,https://www.your-domain.com
#   - http://192.168.1.100:80,http://192.168.1.*:80 (for local network)
#   - For public deployment, use HTTPS!
ALLOWED_ORIGINS=http://localhost
