# Production Environment Configuration
# ==========================================
# IMPORTANT: Update these values for your production deployment
# ==========================================

# Časová zóna
TZ=UTC

# Nastavenie servera
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info

# APP KEY pre AdonisJS
# CRITICAL: Vygeneruj nový kľúč pre production!
# docker compose run --rm backend node ace generate:key
APP_KEY=

# Režim aplikácie
NODE_ENV=production

# Databáza - Pre production použi silné heslá!
DB_HOST=db
DB_PORT=5432
DB_USER=root
DB_PASSWORD=root
DB_DATABASE=app

# CORS - Allowed Origins (comma-separated)
# Pre production špecifikuj presné domény alebo IP adresy
# Príklady:
#   - https://your-domain.com,https://www.your-domain.com
#   - http://192.168.1.100:80,http://192.168.1.*:80 (pre lokálnu sieť)
#   - Pre verejný deployment použi HTTPS!
ALLOWED_ORIGINS=http://localhost
